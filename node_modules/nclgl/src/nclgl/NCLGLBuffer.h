#ifndef NCLGL_NCLGLBUFFER_H
#define NCLGL_NCLGLBUFFER_H

#define GLFW_INCLUDE_ES3
#include <GLFW/glfw3.h>

#include <string.h>
#include <iostream>
#include <cmath>

class NCLGLBuffer {
public:
    NCLGLBuffer();
    ~NCLGLBuffer(void);

    std::string _type;
    GLuint _supportFormat;

    std::string _mode;

    int W;
    int H;
    int length;
    GLuint textureData;
    GLuint textureDataTemp;
    GLuint vertexData0;

    GLuint fBuffer;
    GLuint renderBuffer;
    GLuint fBufferTemp;
    GLuint renderBufferTemp;

    void ini(std::string type, bool linear, std::string mode);
    void createFramebufferAndRenderbuffer();

    void tp();
    void writeWebGLTextureBuffer(float * arr);

    float * prepareArr(float * arr, float * arrD);
    void writeBuffer(float * arr, int valueLength, bool flip, float * overrideDimensions);
    void writeIndices(uint16_t * arr, int valueLength);
    void remove();

private:

};

#endif